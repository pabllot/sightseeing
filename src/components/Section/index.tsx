import Map from "../Map";
import styles from "./styles.module.scss";

const Section = () => {
  return (
    <div className={styles.container}>
      <Map />
    </div>
  );
};

export default Section;
